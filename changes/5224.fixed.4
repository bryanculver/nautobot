Fixed the cached OpenAPI schema varying with the requesting user's language now that model and serializer metadata is translatable. The schema is generated under the default language, so it remains a stable, locale-independent contract artifact.
