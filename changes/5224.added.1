Added the `LANGUAGES` setting and a `UserDefinedLanguageMiddleware` that activates each user's selected language for their own requests. Nautobot never infers a language from the browser's `Accept-Language` header.
