Added an optional `label` argument to `NavMenuTab`, `NavMenuGroup`, and `NavMenuItem`, separating a nav entry's translatable display text from its stable `name` key. Apps that pass only `name` are unaffected and require no changes.
